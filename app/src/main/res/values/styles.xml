    <resources>

        <color name="custom_theme_color">#d6d1c7</color>
        <color name="custom_window_color">#000000</color>
        <color name="custom_action_font_color">#ef4135</color>

        <!-- the theme applied to the application or activity -->
        <style name="AppTheme"
            parent="android:Theme.Holo.Light">
            <item name="android:actionBarStyle">@style/MyActionBar</item>
            <item name="android:actionMenuTextColor">#f7921e</item>
            <item name="android:windowBackground">@color/custom_window_color</item>


        </style>

        <!-- ActionBar styles -->
        <style name="MyActionBar"
            parent="android:Widget.Holo.ActionBar">
            <item name="android:titleTextStyle">@style/MyActionBarTitleText</item>
            <item name="android:background">@color/custom_theme_color</item>
            <item name="android:windowContentOverlay">@null</item>
        </style>

        <!-- ActionBar title text -->
        <style name="MyActionBarTitleText"
            parent="android:TextAppearance.Holo.Widget.ActionBar.Title">
            <item name="android:textColor">@color/custom_action_font_color</item>
        </style>

        <!-- ActionBar tabs text styles -->
        <style name="MyActionBarTabText"
            parent="android:Widget.Holo.ActionBar.TabText">
            <item name="android:textColor">#ffff00</item>
        </style>
    </resources>


